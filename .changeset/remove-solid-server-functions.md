---
"@dom-expressions/runtime": patch
---

Remove the Solid server-function runtime. Frame transport now accepts protocol adapters so Solid can own request dispatch, metadata, CSRF, observers, and progressive enhancement in `@solidjs/web`.
