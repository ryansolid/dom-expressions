---
"@dom-expressions/runtime": patch
---

Behavior claims: a claim-carrying stub landing in a spread on a server-rendered element dev-warns (claims ride named ref/on* positions only — the compiler can't see through spreads), naming the explicit position to write out.
